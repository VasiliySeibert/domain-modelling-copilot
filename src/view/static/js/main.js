// main.js
document.addEventListener('DOMContentLoaded', function() {
    // Initialize app only after DOM is loaded and all other JS files are loaded
    const app = new DomainModellingApp();
    app.initialize();
});